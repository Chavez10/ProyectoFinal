@model IEnumerable<ReservaDeVuelos.Models.PAIS_AEROLINEA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AEROLINEAS.NOM_AEROLINEA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PAISES.PAIS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AEROLINEAS.NOM_AEROLINEA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PAISES.PAIS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_PAIS_AEROLINEA }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_PAIS_AEROLINEA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_PAIS_AEROLINEA })
        </td>
    </tr>
}

</table>
