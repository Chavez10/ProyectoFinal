@model IEnumerable<ReservaDeVuelos.Models.TIPOS_PAGOS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_PAGO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_PAGO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_TIPO_PAGO }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_TIPO_PAGO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_TIPO_PAGO })
        </td>
    </tr>
}

</table>
