@model IEnumerable<ReservaDeVuelos.Models.REGIONES>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.REGION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.REGION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_REGION }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_REGION }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_REGION })
        </td>
    </tr>
}

</table>
