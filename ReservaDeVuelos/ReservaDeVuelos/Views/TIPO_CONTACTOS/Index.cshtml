@model IEnumerable<ReservaDeVuelos.Models.TIPO_CONTACTOS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CONTACTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CONTACTO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_CONTACTO }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_CONTACTO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_CONTACTO })
        </td>
    </tr>
}

</table>
