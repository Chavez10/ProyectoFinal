@model IEnumerable<ReservaDeVuelos.Models.AEROLINEA_AEROPUERTO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PAIS_AEROLINEA1.PAIS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.REGION_AEROPUERTO1.REGION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PAIS_AEROLINEA1.PAIS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.REGION_AEROPUERTO1.REGION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_AL_AP }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_AL_AP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_AL_AP })
        </td>
    </tr>
}

</table>
