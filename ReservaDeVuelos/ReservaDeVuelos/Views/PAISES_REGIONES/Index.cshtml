@model IEnumerable<ReservaDeVuelos.Models.PAISES_REGIONES>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PAISES.PAIS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.REGIONES.REGION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PAISES.PAIS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.REGIONES.REGION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_PAIS_REGION }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_PAIS_REGION }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_PAIS_REGION })
        </td>
    </tr>
}

</table>
