@model IEnumerable<ReservaDeVuelos.Models.TIPO_VUELOS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_VUELO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_VUELO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_TIP_VUELO }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_TIP_VUELO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_TIP_VUELO })
        </td>
    </tr>
}

</table>
